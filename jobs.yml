- name: jenkins jobs
  host: localhost
  connection : local
  vars: 
    jobs: 
      - roboshop-jenkins
      - cart
      - catalogue'
      - user
      - shipping
      - payment
  roles:
    jenkins-jobs